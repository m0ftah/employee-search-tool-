# أداة البحث عن الموظفين - الوثائق الكاملة

## جدول المحتويات
1. [نظرة عامة على المشروع](#نظرة-عامة-على-المشروع)
2. [الهندسة المعمارية ومجموعة التقنيات](#الهندسة-المعمارية-ومجموعة-التقنيات)
3. [هيكل المشروع](#هيكل-المشروع)
4. [نماذج قاعدة البيانات والعلاقات](#نماذج-قاعدة-البيانات-والعلاقات)
5. [أدوار المستخدمين والصلاحيات](#أدوار-المستخدمين-والصلاحيات)
6. [الوثائق لكل ملف](#الوثائق-لكل-ملف)
7. [سير العمل وتدفقات المستخدم](#سير-العمل-وتدفقات-المستخدم)
8. [الميزات الرئيسية](#الميزات-الرئيسية)
9. [الإعدادات](#الإعدادات)
10. [إعداد التطوير](#إعداد-التطوير)

---

## نظرة عامة على المشروع

**أداة البحث عن الموظفين** هي نظام شامل لإدارة التوظيف مبني باستخدام Laravel و Filament. تسهل الاتصال بين متخصصي الموارد البشرية ومرشحي الوظائف، مما يسمح بنشر الوظائف وإدارة الطلبات وتقييم السير الذاتية والتواصل في الوقت الفعلي.

### الوظائف الأساسية
- **إدارة نشر الوظائف**: يمكن لموظفي الموارد البشرية إنشاء وإدارة الوظائف المنشورة
- **تسجيل المرشحين**: يمكن للمرشحين التسجيل وإنشاء ملفات شخصية
- **نظام الطلبات**: يتقدم المرشحون للوظائف مع رفع السير الذاتية
- **تقييم السيرة الذاتية**: التقييم التلقائي لسير المرشحين الذاتية
- **الدردشة الفورية**: المراسلة المباشرة بين الموارد البشرية والمرشحين
- **تتبع الطلبات**: إدارة الحالة (قيد الانتظار، تمت المراجعة، في القائمة المختصرة، مرفوض، تم التوظيف)

---

## الهندسة المعمارية ومجموعة التقنيات

### الواجهة الخلفية
- **الإطار**: Laravel 12.x
- **إصدار PHP**: 8.2+
- **قاعدة البيانات**: MySQL/PostgreSQL (قابل للتكوين)

### الواجهة الأمامية
- **لوحة الإدارة**: Filament 3.3
- **التصميم**: Tailwind CSS
- **JavaScript**: Alpine.js (عبر Filament)
- **أداة البناء**: Vite

### الحزم الرئيسية
- `filament/filament`: إطار عمل لوحة الإدارة
- `bezhansalleh/filament-shield`: التحكم في الوصول القائم على الأدوار
- `namu/wirechat`: وظيفة الدردشة الفورية
- `phpoffice/phpword`: معالجة مستندات Word
- `smalot/pdfparser`: استخراج النص من PDF
- `spatie/laravel-permission`: إدارة الصلاحيات

---

## هيكل المشروع

```
employee-search-tool-/
├── app/
│   ├── Filament/
│   │   ├── Pages/              # صفحات Filament المخصصة
│   │   ├── Resources/           # موارد Filament (واجهات CRUD)
│   │   │   ├── ApplicationResource.php
│   │   │   ├── CandidateResource.php
│   │   │   ├── HRResource.php
│   │   │   ├── JobResource.php
│   │   │   └── UserResource.php
│   │   └── Widgets/             # عناصر لوحة المعلومات
│   ├── Http/
│   │   ├── Controllers/         # وحدات تحكم HTTP
│   │   │   └── CandidateRegistrationController.php
│   │   └── Middleware/           # البرمجيات الوسيطة المخصصة
│   ├── Models/                   # نماذج Eloquent
│   │   ├── Application.php
│   │   ├── Candidate.php
│   │   ├── HR.php
│   │   ├── Job.php
│   │   └── User.php
│   ├── Policies/                 # سياسات التفويض
│   ├── Providers/
│   │   └── Filament/
│   │       └── AdminPanelProvider.php
│   └── Services/                 # خدمات منطق الأعمال
│       └── CVTextExtractorService.php
├── config/                       # ملفات التكوين
├── database/
│   ├── migrations/               # ترحيلات قاعدة البيانات
│   └── seeders/                  # بذور قاعدة البيانات
├── resources/
│   ├── views/                    # قوالب Blade
│   │   ├── candidate/
│   │   │   └── register.blade.php
│   │   └── filament/
│   │       └── components/
│   │           ├── gradient-theme.blade.php
│   │           └── wirechat-styles.blade.php
│   ├── css/
│   └── js/
├── routes/
│   └── web.php                   # مسارات الويب
└── public/                        # الأصول العامة

```

---

## نماذج قاعدة البيانات والعلاقات

### نموذج المستخدم (`app/Models/User.php`)

**الغرض**: نموذج المصادقة المركزي لجميع أنواع المستخدمين.

**السمات الرئيسية**:
- `name`: الاسم الكامل للمستخدم
- `email`: عنوان البريد الإلكتروني
- `password`: كلمة المرور المشفرة
- `type`: نوع المستخدم (`admin`, `hr`, `candidate`)

**العلاقات**:
- `hasOne(HR::class)`: واحد لواحد مع ملف الموارد البشرية
- `hasOne(Candidate::class)`: واحد لواحد مع ملف المرشح
- `HasRoles` trait: كثير إلى كثير مع الأدوار (عبر Spatie Permission)

**الطرق الرئيسية**:
- `isAdmin()`: يُرجع true إذا كان المستخدم مسؤولاً
- `isHR()`: يُرجع true إذا كان المستخدم من الموارد البشرية
- `isCandidate()`: يُرجع true إذا كان المستخدم مرشحاً
- `canCreateChats()`: تجاوز لـ Wirechat - فقط المرشحون والموارد البشرية يمكنهم الدردشة
- `canCreateGroups()`: تجاوز لـ Wirechat - فقط الموارد البشرية يمكنها إنشاء مجموعات
- `searchChatables()`: بحث مخصص يرشح المستخدمين حسب النوع (المرشحون يرون الموارد البشرية، الموارد البشرية ترى المرشحين)

**سير العمل**:
1. يتم إنشاء المستخدم مع حقل `type`
2. بناءً على النوع، يتم إنشاء الملف الشخصي المقابل (الموارد البشرية أو المرشح)
3. يمكن للمستخدم المصادقة والوصول إلى الموارد المناسبة
4. وظيفة الدردشة مقيدة حسب نوع المستخدم

---

### نموذج المرشح (`app/Models/Candidate.php`)

**الغرض**: يخزن معلومات الملف الشخصي الخاصة بالمرشح.

**السمات الرئيسية**:
- `user_id`: المفتاح الخارجي لجدول المستخدمين
- `phone`: رقم الهاتف
- `location`: الموقع الجغرافي
- `resume_path`: مسار ملف السيرة الذاتية المرفوع
- `education_level`: الثانوية، الدبلوم، البكالوريوس، الماجستير، الدكتوراه
- `years_of_experience`: عدد صحيح
- `skills`: مصفوفة JSON للمهارات
- `certifications`: حقل نصي للشهادات
- `bio`: نص السيرة الذاتية
- `score`: تقييم السيرة الذاتية (0-10، محسوب تلقائياً)

**العلاقات**:
- `belongsTo(User::class)`: ينتمي إلى مستخدم واحد
- `hasMany(Application::class)`: لديه العديد من طلبات الوظائف

**سير العمل**:
1. يسجل المرشح عبر مسار `/candidate/register`
2. يتم إنشاء حساب المستخدم مع `type = 'candidate'`
3. يتم إنشاء ملف المرشح الشخصي مع معلومات إضافية
4. يتم رفع السيرة الذاتية ومعالجتها
5. يتم حساب تقييم السيرة الذاتية بناءً على محتوى السيرة الذاتية
6. يمكن للمرشح تصفح الوظائف والتقدم

---

### نموذج الموارد البشرية (`app/Models/HR.php`)

**الغرض**: يخزن معلومات متخصص الموارد البشرية والشركة.

**السمات الرئيسية**:
- `user_id`: المفتاح الخارجي لجدول المستخدمين
- `company_name`: اسم الشركة
- `position`: منصب/لقب الموارد البشرية
- `phone`: رقم الهاتف
- `location`: الموقع الجغرافي
- `company_logo`: مسار شعار الشركة
- `bio`: نص السيرة الذاتية

**العلاقات**:
- `belongsTo(User::class)`: ينتمي إلى مستخدم واحد
- `hasMany(Job::class)`: لديه العديد من الوظائف المنشورة

**سير العمل**:
1. يتم إنشاء حساب الموارد البشرية من قبل المسؤول عبر Filament
2. يتم إنشاء ملف الموارد البشرية مع معلومات الشركة
3. يمكن لموظف الموارد البشرية إنشاء الوظائف المنشورة
4. يمكن لموظف الموارد البشرية عرض وإدارة الطلبات
5. يمكن لموظف الموارد البشرية الدردشة مع المرشحين

---

### نموذج الوظيفة (`app/Models/Job.php`)

**الغرض**: يمثل الوظائف المنشورة التي أنشأتها الموارد البشرية.

**السمات الرئيسية**:
- `hr_id`: المفتاح الخارجي لموظف الموارد البشرية الذي نشر الوظيفة
- `title`: عنوان الوظيفة
- `description`: وصف الوظيفة بنص منسق
- `location`: موقع الوظيفة
- `job_type`: دوام كامل، دوام جزئي، عقد، تدريب
- `salary_range`: نص نطاق الراتب
- `experience_level`: مبتدئ، متوسط، كبير
- `category`: فئة الوظيفة
- `application_deadline`: تاريخ الموعد النهائي
- `status`: نشط أو مغلق

**العلاقات**:
- `belongsTo(HR::class)`: ينتمي إلى موظف موارد بشرية واحد
- `hasMany(Application::class)`: لديه العديد من الطلبات

**سير العمل**:
1. تنشئ الموارد البشرية الوظيفة المنشورة عبر Filament
2. يتم تعيين الوظيفة إلى حالة `active`
3. يمكن للمرشحين عرض والتقدم للوظائف النشطة
4. يتم ربط الطلبات بالوظيفة
5. يمكن لموظف الموارد البشرية إغلاق الوظائف عند شغلها

---

### نموذج الطلب (`app/Models/Application.php`)

**الغرض**: يمثل طلب المرشح لوظيفة.

**السمات الرئيسية**:
- `job_id`: المفتاح الخارجي للوظيفة
- `candidate_id`: المفتاح الخارجي للمرشح
- `resume_path`: مسار السيرة الذاتية (يمكن أن يكون مختلفاً عن سيرة المرشح الشخصية)
- `status`: قيد الانتظار، تمت المراجعة، في القائمة المختصرة، مرفوض، تم التوظيف
- `feedback_from_hr`: نص ملاحظات الموارد البشرية
- `feedback_from_candidate`: نص ملاحظات المرشح
- `applied_at`: الطابع الزمني للطلب
- `score`: تقييم السيرة الذاتية (منسوخ من المرشح أو محسوب)

**العلاقات**:
- `belongsTo(Job::class)`: ينتمي إلى وظيفة واحدة
- `belongsTo(Candidate::class)`: ينتمي إلى مرشح واحد

**سير العمل**:
1. ينقر المرشح على "تقدم" على وظيفة
2. يتم إنشاء الطلب مع `status = 'pending'`
3. يتم رفع السيرة الذاتية (أو استخدام سيرة المرشح الشخصية)
4. تراجع الموارد البشرية الطلب ويمكنها:
   - القبول → تصبح الحالة `shortlisted`
   - الرفض → تصبح الحالة `rejected` (مع ملاحظات)
   - التوظيف → تصبح الحالة `hired` (من القائمة المختصرة)
5. يمكن للمرشح عرض حالة الطلب

---

## أدوار المستخدمين والصلاحيات

### المسؤول
- **الوصول الكامل**: يمكنه إدارة جميع الموارد
- **يمكنه الإنشاء**: المستخدمين، ملفات الموارد البشرية، الوظائف، الطلبات
- **يمكنه التعديل**: جميع السجلات
- **يمكنه الحذف**: جميع السجلات
- **التنقل**: وصول كامل لجميع عناصر القائمة
- **الدردشة**: لا يمكنه استخدام وظيفة الدردشة

### الموارد البشرية
- **يمكنه الإنشاء**: الوظائف المنشورة
- **يمكنه العرض**: 
  - الوظائف المنشورة الخاصة به
  - الطلبات لوظائفه
  - ملفات المرشحين الشخصية
- **يمكنه الإدارة**: حالة الطلب (قبول، رفض، توظيف)
- **يمكنه الدردشة**: مع المرشحين فقط
- **التنقل**: الوظائف، الطلبات، المرشحون، الدردشة

### المرشح
- **يمكنه الإنشاء**: ملفه الشخصي (عبر التسجيل)، الطلبات
- **يمكنه العرض**: 
  - الوظائف المتاحة
  - طلباته
  - ملفات الموارد البشرية الشخصية
- **يمكنه التقدم**: للوظائف النشطة
- **يمكنه الدردشة**: مع الموارد البشرية فقط
- **التنقل**: الوظائف، الطلبات، الدردشة

---

## الوثائق لكل ملف

### وحدات التحكم

#### `app/Http/Controllers/CandidateRegistrationController.php`

**الغرض**: يتعامل مع تسجيل المرشحين خارج Filament.

**الطرق الرئيسية**:
- `showRegistrationForm()`: يعرض نموذج التسجيل
- `register(Request $request)`: يعالج التسجيل

**سير العمل**:
1. يزور المستخدم `/candidate/register`
2. يتم عرض النموذج مع حقول الحساب والملف الشخصي
3. عند الإرسال:
   - يتم إنشاء حساب المستخدم
   - يتم إنشاء ملف المرشح الشخصي
   - يتم رفع السيرة الذاتية وتخزينها
   - يتم استخراج نص السيرة الذاتية
   - يتم تسجيل دخول المستخدم
   - يتم إعادة التوجيه إلى لوحة معلومات Filament

**قواعد التحقق**:
- الاسم، البريد الإلكتروني، كلمة المرور مطلوبة
- يجب أن يكون البريد الإلكتروني فريداً
- يجب أن تكون السيرة الذاتية PDF أو DOC أو DOCX
- يتم التحقق من مستوى التعليم وسنوات الخبرة

---

### الخدمات

#### `app/Services/CVTextExtractorService.php`

**الغرض**: يستخرج محتوى النص من ملفات السيرة الذاتية (PDF، DOC، DOCX) للتقييم.

**الطرق الرئيسية**:
- `extractText(string $filePath)`: الطريقة الرئيسية التي تحدد نوع الملف وتستخرج النص
- `extractFromPdf(string $filePath)`: يستخرج النص من PDF باستخدام Smalot PDF Parser
- `extractFromWord(string $filePath)`: يستخرج النص من مستندات Word باستخدام PhpOffice

**سير العمل**:
1. يتم توفير مسار الملف
2. يتم اكتشاف امتداد الملف
3. يتم استدعاء طريقة الاستخراج المناسبة
4. يتم تنظيف النص (تطبيع المسافات البيضاء)
5. يتم إرجاع النص للتقييم

**الاستخدام**:
```php
$extractor = new CVTextExtractorService();
$text = $extractor->extractText('resumes/cv.pdf');
// استخدم $text لخوارزمية التقييم
```

---

### موارد Filament

#### `app/Filament/Resources/ApplicationResource.php`

**الغرض**: يدير طلبات الوظائف في لوحة الإدارة.

**الميزات الرئيسية**:
- **أعمدة الجدول**:
  - عنوان الوظيفة، اسم الشركة، اسم/بريد المرشح
  - تقييم السيرة الذاتية (شارة ملونة)
  - رابط السيرة الذاتية/السيرة
  - شارة الحالة
  - ملاحظات الموارد البشرية، تاريخ التقديم

- **المرشحات**:
  - الحالة (اختيار متعدد)
  - عنوان الوظيفة (مرشح علاقة)
  - المرشح (مرشح علاقة)
  - اسم الشركة (مرشح علاقة)
  - نطاق تقييم السيرة الذاتية
  - نطاق تاريخ التقديم
  - لديه سيرة ذاتية (تبديل)
  - لديه ملاحظات من الموارد البشرية (تبديل)

- **الإجراءات**:
  - **قبول**: ينقل الطلب إلى `shortlisted` (الموارد البشرية فقط)
  - **توظيف**: ينقل من `shortlisted` إلى `hired` (الموارد البشرية فقط)
  - **رفض**: ينقل إلى `rejected` مع نموذج الملاحظات (الموارد البشرية فقط)
  - **تعديل**: تعديل كامل (المسؤول فقط)
  - **حذف**: حذف الطلب (المسؤول فقط)

**التفويض**:
- `canViewAny()`: المسؤول، الموارد البشرية (وظائفه)، المرشحون (طلباته)
- `canView()`: المسؤول، الموارد البشرية (وظائفه)، المرشحون (طلباته)
- `canEdit()`: المسؤول فقط
- `canCreate()`: خطأ (يتم إنشاء الطلبات عبر تدفق طلب الوظيفة)

**سير العمل**:
1. يظهر الطلب في الجدول بعد تقديم المرشح
2. ترشح الموارد البشرية حسب الوظيفة أو الحالة
3. تراجع الموارد البشرية السيرة الذاتية والتقييم
4. تتخذ الموارد البشرية إجراء (قبول/رفض/توظيف)
5. يرى المرشح تحديث الحالة

---

#### `app/Filament/Resources/CandidateResource.php`

**الغرض**: يدير ملفات المرشحين الشخصية.

**الميزات الرئيسية**:
- **أعمدة الجدول**:
  - الاسم، البريد الإلكتروني، الهاتف، الموقع
  - مستوى التعليم (شارة)
  - سنوات الخبرة
  - تقييم السيرة الذاتية (ملون مع أيقونات)
  - رابط السيرة الذاتية
  - عدد الطلبات

- **المرشحات**:
  - مستوى التعليم (اختيار متعدد)
  - نطاق تقييم السيرة الذاتية
  - نطاق سنوات الخبرة
  - لديه سيرة ذاتية (تبديل)
  - لديه طلبات (تبديل)
  - بحث الموقع
  - بحث الهاتف
  - نطاق تاريخ الإنشاء

- **الإجراءات**:
  - **دردشة**: يفتح الدردشة مع المرشح (الموارد البشرية فقط)
  - **تعديل**: تعديل ملف المرشح الشخصي
  - **حذف**: حذف المرشح

**التفويض**:
- يمكن لجميع المستخدمين المصادق عليهم العرض
- يمكن للمسؤول التعديل/الحذف
- يمكن للموارد البشرية العرض والدردشة

**سير العمل**:
1. يسجل المرشح → يظهر الملف الشخصي في الجدول
2. تبحث الموارد البشرية/ترشح المرشحين
3. تعرض الموارد البشرية تفاصيل المرشح
4. يمكن للموارد البشرية بدء الدردشة أو عرض الطلبات

---

#### `app/Filament/Resources/HRResource.php`

**الغرض**: يدير ملفات الموارد البشرية الشخصية والشركات.

**الميزات الرئيسية**:
- **أعمدة الجدول**:
  - الاسم، البريد الإلكتروني، اسم الشركة، المنصب
  - الهاتف، الموقع
  - شعار الشركة (صورة دائرية)
  - عدد الوظائف

- **المرشحات**:
  - بحث اسم الشركة
  - بحث المنصب
  - بحث الموقع
  - بحث الهاتف
  - لديه شعار (تبديل)
  - لديه وظائف (تبديل)
  - نطاق عدد الوظائف
  - نطاق تاريخ الإنشاء

- **الإجراءات**:
  - **دردشة**: يفتح الدردشة مع الموارد البشرية (المرشح فقط)
  - **تعديل**: تعديل ملف الموارد البشرية الشخصي
  - **حذف**: حذف الموارد البشرية

**التفويض**:
- يمكن لجميع المستخدمين المصادق عليهم العرض
- يمكن للمسؤول الإنشاء/التعديل/الحذف
- يمكن للمرشحين الدردشة

**سير العمل**:
1. ينشئ المسؤول حساب الموارد البشرية
2. يتم إنشاء ملف الموارد البشرية الشخصي
3. تنشئ الموارد البشرية الوظائف
4. يمكن للمرشحين عرض ملفات الموارد البشرية الشخصية والدردشة

---

#### `app/Filament/Resources/JobResource.php`

**الغرض**: يدير الوظائف المنشورة.

**الميزات الرئيسية**:
- **أعمدة الجدول**:
  - اسم الشركة، عنوان الوظيفة، الموقع
  - نوع الوظيفة (شارة)، نطاق الراتب
  - مستوى الخبرة (شارة ملونة)
  - الفئة، الوصف (مختصر)
  - موعد نهائي للطلب، الحالة (شارة)
  - عدد الطلبات
  - تم التقديم (للمرشحين)

- **المرشحات**:
  - الحالة (اختيار متعدد)
  - نوع الوظيفة (اختيار متعدد)
  - مستوى الخبرة (اختيار متعدد)
  - اسم الشركة (مرشح علاقة)
  - بحث عنوان الوظيفة
  - بحث الموقع
  - بحث الفئة
  - بحث نطاق الراتب
  - نطاق موعد نهائي للطلب
  - لديه طلبات (تبديل)
  - نطاق عدد الطلبات
  - انتهى الموعد النهائي (تبديل)
  - نطاق تاريخ الإنشاء

- **الإجراءات**:
  - **تقدم**: يتقدم المرشح للوظيفة (يفتح نافذة منبثقة مع تفاصيل الوظيفة ورفع السيرة الذاتية)
  - **تعديل**: تعديل الوظيفة (المسؤول/الموارد البشرية)
  - **حذف**: حذف الوظيفة (المسؤول/الموارد البشرية)

**التفويض**:
- يمكن لجميع المستخدمين المصادق عليهم العرض
- يمكن للمسؤول والموارد البشرية الإنشاء/التعديل/الحذف
- يمكن للمرشحين التقدم

**سير العمل**:
1. تنشئ الموارد البشرية الوظيفة المنشورة
2. تظهر الوظيفة في الجدول مع حالة `active`
3. يتصفح المرشحون ويرشحون الوظائف
4. ينقر المرشح على "تقدم"
5. تظهر نافذة منبثقة مع تفاصيل الوظيفة
6. يرفع المرشح السيرة الذاتية أو يستخدم سيرته الشخصية
7. يتم إنشاء الطلب

---

#### `app/Filament/Resources/UserResource.php`

**الغرض**: يدير حسابات المستخدمين.

**الميزات الرئيسية**:
- **أعمدة الجدول**:
  - الاسم، البريد الإلكتروني
  - النوع (شارة ملونة: admin=خطر، hr=تحذير، candidate=نجاح)
  - الأدوار (شارات)
  - تم التحقق من البريد الإلكتروني (أيقونة)

- **المرشحات**:
  - النوع (اختيار متعدد)
  - بحث الاسم
  - بحث البريد الإلكتروني
  - تم التحقق من البريد الإلكتروني (تبديل)
  - لديه أدوار (تبديل)
  - الأدوار (مرشح علاقة)
  - نطاق تاريخ الإنشاء

- **الإجراءات**:
  - **تعديل**: تعديل المستخدم
  - **حذف**: حذف المستخدم

**التفويض**:
- المسؤول فقط

**سير العمل**:
1. ينشئ المسؤول حساب المستخدم
2. يتم تعيين نوع المستخدم
3. يتم إنشاء الملف الشخصي المقابل (الموارد البشرية/المرشح) بشكل منفصل
4. يمكن للمستخدم المصادقة والوصول إلى النظام

---

### السياسات

توجد السياسات في `app/Policies/` وتتحكم في التفويض:

- **ApplicationPolicy**: يتحكم في من يمكنه عرض/تعديل/حذف الطلبات
- **CandidatePolicy**: يتحكم في الوصول إلى مورد المرشح
- **HRPolicy**: يتحكم في الوصول إلى مورد الموارد البشرية
- **JobPolicy**: يتحكم في الوصول إلى مورد الوظيفة
- **UserPolicy**: يتحكم في الوصول إلى مورد المستخدم
- **RolePolicy**: يتحكم في الوصول إلى إدارة الأدوار

كل سياسة تنفذ طرق التفويض القياسية في Laravel:
- `viewAny()`, `view()`, `create()`, `update()`, `delete()`

---

### العروض

#### `resources/views/candidate/register.blade.php`

**الغرض**: صفحة نموذج تسجيل المرشح.

**الميزات**:
- نموذج من قسمين:
  1. معلومات الحساب (الاسم، البريد الإلكتروني، كلمة المرور)
  2. معلومات الملف الشخصي (الهاتف، الموقع، التعليم، الخبرة، المهارات، السيرة الذاتية، الشهادات، السيرة)
- رفع ملف للسيرة الذاتية
- ملاحظات التحقق
- تصميم سمة متدرجة
- تصميم متجاوب

**سير العمل**:
1. يزور المستخدم `/candidate/register`
2. يتم عرض النموذج
3. يملأ المستخدم المعلومات
4. عند الإرسال، يحدث التحقق
5. إذا كان صحيحاً، يتم معالجة التسجيل
6. يتم إعادة توجيه المستخدم إلى لوحة المعلومات

---

#### `resources/views/filament/components/gradient-theme.blade.php`

**الغرض**: يطبق سمة متدرجة من البترولي إلى البرتقالي على لوحة معلومات Filament.

**الميزات**:
- متغيرات CSS للألوان المتدرجة
- تصميم مخصص لمكونات Filament
- دعم الوضع الداكن
- مطبق عالمياً على لوحة الإدارة

---

#### `resources/views/filament/components/wirechat-styles.blade.php`

**الغرض**: تصميم مخصص لواجهة Wirechat.

**الميزات**:
- واجهة مستخدم مبسطة وعصرية
- تصميم نافذة منبثقة مخصصة لـ "دردشة جديدة"
- تصميم قائمة المستخدمين
- تصميم فقاعات الرسائل
- دعم الوضع الداكن
- JavaScript للتصميم الديناميكي

**التصميم الرئيسي**:
- خلفية نافذة منبثقة مع ضبابية
- حاويات محتوى بيضاء
- زوايا مستديرة
- رسوم متحركة سلسة
- تصميم متجاوب

---

### الإعدادات

#### `app/Providers/Filament/AdminPanelProvider.php`

**الغرض**: يكوّن لوحة معلومات Filament.

**الإعدادات الرئيسية**:
- معرف اللوحة: `admin`
- المسار: `/admin`
- النطاق: null (جميع النطاقات)
- اسم العلامة التجارية والشعار
- مجموعات التنقل
- سمة مخصصة
- تسجيل العناصر
- أنماط ونصوص مخصصة

**المكونات المخصصة**:
- `gradient-theme.blade.php`: تصميم السمة
- `wirechat-styles.blade.php`: تصميم الدردشة

---

### المسارات

#### `routes/web.php`

**المسارات الرئيسية**:
- `/`: صفحة الترحيب
- `/candidate/register`: نموذج تسجيل المرشح
- `/admin/*`: لوحة معلومات Filament (جميع الموارد)
- `/chats/*`: مسارات Wirechat (مكوّنة في `config/wirechat.php`)

**البرمجيات الوسيطة**:
- `web`: الجلسة، حماية CSRF
- `auth`: المصادقة مطلوبة للوحة الإدارة

---

## سير العمل وتدفقات المستخدم

### تدفق تسجيل المرشح

1. **يزور المستخدم** `/candidate/register`
2. **يملأ النموذج**:
   - معلومات الحساب (الاسم، البريد الإلكتروني، كلمة المرور)
   - معلومات الملف الشخصي (الهاتف، الموقع، التعليم، الخبرة، المهارات، السيرة الذاتية، إلخ)
3. **يرسل النموذج**
4. **تتولى الواجهة الخلفية المعالجة**:
   - ينشئ المستخدم مع `type = 'candidate'`
   - ينشئ ملف المرشح الشخصي
   - يرفع السيرة الذاتية إلى التخزين
   - يستخرج النص من السيرة الذاتية (للتقييم المستقبلي)
   - يسجل دخول المستخدم
5. **يعيد التوجيه** إلى لوحة المعلومات `/admin`
6. **يمكن للمرشح الآن**:
   - تصفح الوظائف
   - التقدم للوظائف
   - عرض الطلبات
   - الدردشة مع الموارد البشرية

---

### تدفق طلب الوظيفة

1. **يتصفح المرشح** الوظائف في جدول JobResource
2. **يرشح** حسب الموقع، النوع، مستوى الخبرة، إلخ
3. **ينقر على "تقدم"** على وظيفة
4. **تفتح نافذة منبثقة** تعرض:
   - تفاصيل الوظيفة (الشركة، الموقع، النوع، الراتب، الموعد النهائي، الوصف)
   - خيارات السيرة الذاتية:
     - استخدام سيرة الملف الشخصي الموجودة (إن وجدت)
     - رفع سيرة ذاتية جديدة
5. **يختار المرشح** خيار السيرة الذاتية
6. **يرسل الطلب**
7. **تنشئ الواجهة الخلفية** سجل الطلب:
   - `job_id`: الوظيفة المختارة
   - `candidate_id`: المرشح الحالي
   - `resume_path`: مسار ملف السيرة الذاتية
   - `status`: `pending`
   - `applied_at`: الطابع الزمني الحالي
8. **يظهر الطلب** في جدول ApplicationResource
9. **تراجع الموارد البشرية** الطلب:
   - تعرض السيرة الذاتية والتقييم
   - تتخذ إجراء:
     - **قبول** → `shortlisted`
     - **رفض** → `rejected` (مع ملاحظات)
     - **توظيف** → `hired` (من القائمة المختصرة)
10. **يرى المرشح** تحديث الحالة في طلباته

---

### تدفق الدردشة

1. **ينقر الموارد البشرية أو المرشح** على زر "دردشة" على ملف شخصي
2. **تتحقق الواجهة الخلفية**:
   - يمكن للموارد البشرية الدردشة مع المرشحين فقط
   - يمكن للمرشحين الدردشة مع الموارد البشرية فقط
3. **يتم إنشاء المحادثة** (أو استرجاعها إن كانت موجودة)
4. **يتم إعادة توجيه المستخدم** إلى `/chats/{conversation_id}`
5. **تحمّل واجهة Wirechat**:
   - قائمة المستخدمين (مرشحة حسب النوع)
   - سجل الرسائل
   - حقل الإدخال
6. **يرسل المستخدمون الرسائل** في الوقت الفعلي
7. **يتم تخزين الرسائل** في قاعدة البيانات
8. **يمكن إرسال الإشعارات** (إذا تم تكوينها)

---

### تدفق إدارة وظائف الموارد البشرية

1. **يسجل دخول الموارد البشرية** إلى `/admin`
2. **يتنقل** إلى "الوظائف المنشورة"
3. **ينشئ الوظيفة**:
   - يملأ العنوان، الوصف، الموقع، النوع، الراتب، مستوى الخبرة، الفئة، الموعد النهائي
   - يضبط الحالة على `active`
4. **تظهر الوظيفة** في الجدول
5. **يتقدم المرشحون** (انظر تدفق طلب الوظيفة)
6. **تعرض الموارد البشرية الطلبات**:
   - ترشح حسب الوظيفة، الحالة، التقييم، إلخ
   - تراجع السير الذاتية
   - تتخذ إجراءات (قبول/رفض/توظيف)
7. **يمكن للموارد البشرية إغلاق** الوظيفة عند شغلها

---

## الميزات الرئيسية

### نظام تقييم السيرة الذاتية

**الغرض**: تقييم تلقائي لسير المرشحين الذاتية بناءً على المحتوى.

**التنفيذ**:
- يتم استخراج النص من السيرة الذاتية (PDF/DOC/DOCX)
- تحلل خوارزمية التقييم:
  - الكلمات الرئيسية المطابقة لمتطلبات الوظيفة
  - مستوى التعليم
  - سنوات الخبرة
  - المهارات المذكورة
  - الشهادات
- يتم تخزين التقييم في `candidates.score` و `applications.score`
- نطاق التقييم: 0-10
- الترميز اللوني:
  - 8-10: أخضر (نجاح)
  - 6-7.9: أصفر (تحذير)
  - 4-5.9: أزرق (معلومات)
  - 0-3.9: رمادي

**ملاحظة**: قد يكون تنفيذ خوارزمية التقييم الكاملة في خدمة أو قائمة انتظار وظائف.

---

### نظام المرشحات

جميع الموارد لديها مرشحات شاملة:

**أنواع المرشحات الشائعة**:
- **SelectFilter**: قائمة منسدلة مع خيارات (يمكن أن تكون متعددة)
- **Filter**: نموذج مخصص مع حقول نصية، منتقي تواريخ، إلخ
- **Toggle Filter**: منطقي تشغيل/إيقاف

**تخطيط المرشح**:
- `AboveContentCollapsible`: تظهر المرشحات فوق الجدول، يمكن طيها

**الفوائد**:
- تصفية سريعة للبيانات
- مجموعات مرشحات متعددة
- حالات مرشحات محفوظة
- تصدير البيانات المصفاة

---

### نظام رفع الملفات

**رفع السير الذاتية**:
- **الموقع**: `storage/app/public/resumes/` (ملفات المرشحين الشخصية)
- **الموقع**: `storage/app/public/application-resumes/` (طلبات الوظائف)
- **الأنواع المقبولة**: PDF، DOC، DOCX
- **الحجم الأقصى**: 10MB (قابل للتكوين)
- **الرؤية**: عامة (يمكن الوصول إليها عبر URL)

**شعارات الشركات**:
- **الموقع**: `storage/app/public/company-logos/`
- **الأنواع المقبولة**: الصور (JPG، PNG، إلخ)
- **الرؤية**: عامة

**المعالجة**:
- يتم تخزين الملفات عبر Laravel Storage
- يتم حفظ المسارات في قاعدة البيانات
- يمكن الوصول إلى الملفات عبر `asset('storage/path')`

---

### الدردشة الفورية (Wirechat)

**التكوين**: `config/wirechat.php`

**الميزات**:
- محادثات فردية
- محادثات جماعية (الموارد البشرية فقط)
- سجل الرسائل
- مرفقات الملفات (إذا تم تمكينها)
- إيصالات القراءة (إذا تم تمكينها)
- وظيفة البحث

**القيود**:
- فقط المرشحون والموارد البشرية يمكنهم الدردشة
- يمكن للمرشحين الدردشة مع الموارد البشرية فقط
- يمكن للموارد البشرية الدردشة مع المرشحين فقط
- لا يمكن للمسؤولين الدردشة

**التخصيص**:
- تصميم مخصص عبر `wirechat-styles.blade.php`
- بحث مخصص عبر `User::searchChatables()`
- أسماء عرض وأفاتار مخصصة

---

## الإعدادات

### متغيرات البيئة

متغيرات `.env` الرئيسية:
```
APP_NAME="أداة البحث عن الموظفين"
APP_URL=http://localhost:8000
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=employee_search
DB_USERNAME=root
DB_PASSWORD=

FILAMENT_DARK_MODE=false
```

### تكوين Filament

- **معرف اللوحة**: `admin`
- **المسار**: `/admin`
- **السمة**: سمة متدرجة مخصصة
- **التنقل**: مجمعة حسب إدارة المستخدمين والوظائف

### تكوين Wirechat

- **بادئة المسارات**: `chats`
- **نموذج المستخدم**: `App\Models\User`
- **الحقول القابلة للبحث**: `name`، `email`
- **الحد الأقصى للمشاركين**: قابل للتكوين

---

## إعداد التطوير

### المتطلبات الأساسية
- PHP 8.2+
- Composer
- Node.js & NPM
- MySQL/PostgreSQL
- Git

### خطوات التثبيت

1. **استنساخ المستودع**
```bash
git clone <repository-url>
cd employee-search-tool-
```

2. **تثبيت التبعيات**
```bash
composer install
npm install
```

3. **إعداد البيئة**
```bash
cp .env.example .env
php artisan key:generate
```

4. **تكوين قاعدة البيانات**
قم بتحرير `.env` مع بيانات اعتماد قاعدة البيانات الخاصة بك:
```
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=employee_search
DB_USERNAME=root
DB_PASSWORD=
```

5. **تشغيل الترحيلات**
```bash
php artisan migrate
```

6. **بذر قاعدة البيانات** (اختياري)
```bash
php artisan db:seed
```

7. **إنشاء رابط التخزين**
```bash
php artisan storage:link
```

8. **بناء الأصول**
```bash
npm run build
# أو للتطوير:
npm run dev
```

9. **بدء خادم التطوير**
```bash
# الخيار 1: استخدام سكريبت Composer (موصى به)
composer dev

# الخيار 2: يدوي
php artisan serve --host=0.0.0.0
php artisan queue:listen
npm run dev
```

### إنشاء مستخدم مسؤول

قم بتشغيل البذرة:
```bash
php artisan db:seed --class=AdminUserSeeder
```

أو أنشئ يدوياً:
1. سجل كمرشح أولاً
2. غيّر نوع المستخدم إلى `admin` في قاعدة البيانات
3. عيّن دور المسؤول عبر Filament Shield

### نقاط الوصول

- **لوحة الإدارة**: `http://localhost:8000/admin`
- **تسجيل المرشح**: `http://localhost:8000/candidate/register`
- **واجهة الدردشة**: `http://localhost:8000/chats`

---

## مخطط قاعدة البيانات

### الجداول

1. **users**
   - id, name, email, password, type, email_verified_at, created_at, updated_at

2. **candidates**
   - id, user_id, phone, location, resume_path, education_level, years_of_experience, skills (JSON), certifications, bio, score, created_at, updated_at

3. **hrs**
   - id, user_id, company_name, position, phone, location, company_logo, bio, created_at, updated_at

4. **job_postings**
   - id, hr_id, title, description, location, job_type, salary_range, experience_level, category, application_deadline, status, created_at, updated_at

5. **applications**
   - id, job_id, candidate_id, resume_path, status, feedback_from_hr, feedback_from_candidate, applied_at, score, created_at, updated_at

6. **wirechat_*** (جداول حزمة Wirechat)
   - conversations, messages, participants, attachments, إلخ

7. **permission_*** (جداول Spatie Permission)
   - roles, permissions, model_has_roles, إلخ

---

## اعتبارات الأمان

1. **المصادقة**: مصادقة Laravel المدمجة
2. **التفويض**: السياسات و Filament Shield
3. **حماية CSRF**: مفعلة على جميع النماذج
4. **التحقق من رفع الملفات**: قيود النوع والحجم
5. **حقن SQL**: محمي بواسطة Eloquent ORM
6. **حماية XSS**: قوالب Blade تهرب من الإخراج
7. **تشفير كلمة المرور**: Bcrypt افتراضياً

---

## استكشاف الأخطاء وإصلاحها

### المشاكل الشائعة

1. **رابط التخزين لا يعمل**
   ```bash
   php artisan storage:link
   ```

2. **أخطاء رفض الصلاحيات**
   ```bash
   chmod -R 775 storage bootstrap/cache
   ```

3. **قائمة الانتظار لا تعالج**
   ```bash
   php artisan queue:listen
   ```

4. **الأصول لا تحمّل**
   ```bash
   npm run build
   php artisan optimize:clear
   ```

5. **الدردشة لا تعمل**
   - تحقق من تكوين Wirechat
   - تحقق من أنواع المستخدمين (فقط الموارد البشرية والمرشحون)
   - تحقق من ترحيلات قاعدة البيانات

---

## التحسينات المستقبلية

التحسينات المحتملة:
- إشعارات البريد الإلكتروني للطلبات
- خوارزمية تقييم متقدمة للسيرة الذاتية
- توصيات الوظائف للمرشحين
- لوحة معلومات تحليلات الطلبات
- وظيفة التصدير (PDF، Excel)
- دعم متعدد اللغات
- نقاط نهاية API لتطبيق الهاتف المحمول
- إشعارات فورية
- جدولة مقابلات الفيديو
- تكامل توقيع المستندات

---

## الدعم والمساهمة

للأسئلة أو المساهمات:
1. تحقق من الوثائق الموجودة
2. راجع تعليقات الكود
3. اختبر في بيئة التطوير
4. اتبع أفضل ممارسات Laravel و Filament

---

## الترخيص

هذا المشروع برنامج مفتوح المصدر مرخص بموجب [ترخيص MIT](https://opensource.org/licenses/MIT).

---

**إصدار الوثائق**: 1.0  
**آخر تحديث**: 2024  
**المحتفظ به بواسطة**: فريق التطوير
